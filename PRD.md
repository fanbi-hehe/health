# 私人增重助手 —— 项目需求文档（PRD）V4.1

| 版本 | 日期 | 修改说明 |
| :--- | :--- | :--- |
| V1.0 | 2026-07-20 | 初始草案 |
| V3.0 | 2026-08-05 | 完整整合饮食/训练/每日评估/对话Agent/数据看板 |
| V4.0 | 2026-08-06 | 新增完整设置中心、自定义食物库CRUD、数据备份恢复、训练休息倒计时、负向激励通知、桌面小组件。确立"本地智能路由+精准数据注入"为AI对话核心原则 |
| **V4.1** | **2026-08-06** | **UI框架由XML+Fragment全面切换为Jetpack Compose + Material 3；图表库由MPAndroidChart变更为Vico；聊天界面由RecyclerView变更为LazyColumn** |

---

## 1. 项目背景与目标

市面上增重/健身App要么收费昂贵，要么功能冗余，且AI识别受限于黑盒算法。用户希望拥有一款**完全免费、数据私有、高度自定义**的本地工具，同时借助通用大模型实现智能饮食记录、训练指导与自由健康问答。

**核心哲学：**
- **离线优先，数据主权在手**：除AI识别和对话调用API外，所有数据均在本地，不上传任何第三方服务器。
- **AI是副驾驶，而非驾驶员**：AI仅用于视觉识别与自然语言建议，所有逻辑判断、数据统计、意图路由均由本地代码高效完成。
- **高度自定义，不为任何人做减法**：从API Key到食物热量，从训练计划到通知策略，一切皆可配置。

**核心目标**：通过拍照记录饮食（AI估算+人工修正）、结构化手动记录训练，结合AI每日综合评估与自由对话Agent，帮助使用者清晰掌握每日热量盈余、训练容量与长期趋势，最终实现科学增肌增重。

---

## 2. 目标用户与典型场景

- **用户**：个人开发者/健身爱好者（Android用户，追求极简与数据掌控，预算允许调用AI API）。
- **典型场景**：
  - 三餐前拍照 → 确认食物重量与热量后保存。
  - 训练后即时记录部位、动作、组数、次数、重量，组间休息时一键开启倒计时。
  - 睡前点击"今日综合评估"，获取AI基于当日饮食与训练的个性化建议。
  - 有任何饮食、训练疑问时，随时在"对话"Tab向AI助手提问，助手通过本地意图路由**自动精准查询**相关数据后给出回复（而非塞入全量历史）。
  - 定期查看体重曲线与热量趋势图，追踪增重进度。
  - 在设置页自由更换API Key和模型、添加常吃食物、备份全部数据。

---

## 3. 核心功能需求（共七大模块）

### 模块一：饮食记录（AI拍照 + 人工确认修正 + 模板）

1. **拍照入口**：主页面显眼位置，调用系统相机或CameraX。
2. **图像压缩**：拍照后自动压缩至**长边1080px，质量70%**（约100-300KB），减少流量消耗。
3. **AI识别**：调用GLM-4V-Flash，Prompt强制返回纯JSON。容错：若返回非标准JSON，自动提取首尾花括号内容，失败则转手动输入。
4. **确认修改界面**：
   - 展示每个识别出的食物项（可编辑名称、重量、热量）。
   - 重量快捷调整按钮（±10g / ±50g）。
   - 热量可手动修改，也可按修改后的重量联动重算（基于内置/自定义食物库的热量密度）。
   - 底部显示总热量。
   - 选择餐别（早/午/晚/加餐），确认后保存入库。
   - **新增**："存为模板"按钮，可将当前餐食组合保存为自定义模板。
5. **手动快速录入**：
   - 同等优先级入口，直接输入食物名、重量、热量、餐别。
   - **输入食物名时自动补全**：同时检索内置食物库（约200种常见食物）和用户自定义食物库，匹配项自动填入热量密度，自定义食物优先显示。
   - **快捷模板**：主页或饮食页顶部提供"模板"下拉菜单，点击"我的早餐模板"一键填入全部食物项，仅需微调重量即可保存。
6. **数据存储**：Room数据库 `DietRecord` 表。

---

### 模块二：训练记录（结构化日志 + 历史推荐 + 休息倒计时）

1. **记录内容**：训练日期、部位标签（多选：胸、背、腿、肩、手臂、核心）、动作名称、组数、每组次数、配重(kg)、可选备注（如RPE自评）。
2. **历史动作推荐**：选择部位后，动作名称输入框自动显示该部位历史用过的动作列表，点击即填入。
3. **组间休息倒计时（新增）**：
   - 训练记录页面底部固定悬浮圆环倒计时组件。
   - 默认60秒，支持±5秒微调。
   - 点击"开始休息"，圆环动画倒计时，结束时**手机震动**并播放系统提示音。
   - 倒计时期间不阻塞UI操作，支持后台继续计时（通过通知栏常驻）。
   - **小米HyperOS适配**：利用高优先级通知模拟"灵动岛"效果，倒计时在屏幕顶部挖孔区域展示。
4. **数据存储**：Room数据库 `TrainingRecord` 表。

---

### 模块三：目标设定

1. **目标设置页**：输入目标体重(kg)、每日目标摄入热量(kcal)。
2. **主页顶部进度条**：显示"已摄入 / 目标热量（如 2100 / 2800 kcal）"及百分比进度。
3. **配置存储**：使用 `DataStore` 持久化保存。

---

### 模块四：AI每日综合评估（快捷复盘）

1. **入口**：主页或看板页的"今日综合评估"按钮。
2. **实现逻辑**：
   - 查询今日饮食记录、今日训练记录、近3-5天体重记录。
   - 组装成自然语言上下文（**仅最近3天详细数据，更早数据只传SQL算好的统计值**）。
   - 调用GLM-4/DeepSeek文本模型，返回200字以内的评估与建议。
3. **数据保存**：结果存入 `AdviceLog` 表，支持历史查看。

---

### 模块五：AI对话Agent（自由问答 —— 基于本地智能路由 + 精准数据注入）

这是V4.0的核心升级，彻底颠覆传统"全量上下文投喂"模式。

1. **入口**：底部导航栏独立"对话"Tab。
2. **交互方式**：类微信聊天界面，支持多轮对话，聊天历史本地存储可翻看。
3. **核心原则 —— "本地路由，按需查询"（拒绝全量投喂）**：
   - **用户发送消息后，App不立即发送全量数据**，而是先在本地通过**关键词匹配（正则/Kotlin逻辑）** 判断用户意图。
   - **仅查询**与意图强相关的数据：
     - 问"深蹲重量" → 只查 `TrainingRecord` 表中动作名为"深蹲"的历史记录。
     - 问"今天热量" → 只查今日 `DietRecord` 的总和与明细。
     - 问"最近整体怎么样" → 查最近3天精简摘要 + SQL算好的30天平均体重/热量变化。
   - **长短期记忆分层**：
     - **近期（最近3天）**：提供详细清单。
     - **远期（7天以上）**：由本地SQL直接计算统计值（如平均值、总和、变化趋势），以纯文本数字注入。**严禁将原始数据或聊天记录发往AI进行总结。**
   - **滑动窗口**：API调用仅保留最近10条聊天记录作为多轮对话背景。
4. **上下文注入示例**：
   ```
   用户近期数据（本地精准查询结果）：
   - 今日摄入：2100 kcal（目标2800 kcal）
   - 深蹲历史：最近5次 50kg/55kg/55kg/60kg/60kg
   - 过去30天体重变化：+0.5kg
   （仅注入以上精准数据，不注入无关的闲聊历史）
   ```
5. **数据存储**：Room数据库 `ChatMessage` 表（角色、内容、时间戳），UI显示全部历史，但API调用仅截取最近10条。

---

### 模块六：数据看板与趋势

1. **体重录入**：手动输入体重(kg)+日期，存入Room `BodyWeight` 表。
2. **趋势图表**：集成Vico图表库（Compose原生），展示体重变化折线图（7天/30天/90天切换）和每日热量摄入柱状图。
3. **未来扩展**：智能周报（自动融合饮食、训练、体重变化生成总结）。

---

### 模块七：设置与自定义中心（全新模块）

这是V4.0的灵魂，所有自定义配置汇聚于此。

#### 7.1 模型服务配置
- **API Base URL**（可编辑，支持切换GLM/DeepSeek/OpenAI等，默认智谱）
- **API Key**（输入框，默认隐藏，支持显示/隐藏）
- **文本模型名称**（默认 `glm-4-flash` 或 `deepseek-chat`）
- **视觉模型名称**（默认 `glm-4v-flash`）
- 安全提示："密钥仅保存在本机，请勿泄露。" —— 所有配置存入 `DataStore`。

#### 7.2 自定义食物热量表（CRUD）
- 进入"食物管理"页面，展示所有用户自定义食物列表。
- 支持**添加、编辑、删除**自定义食物（名称 + 每100g热量）。
- 添加的食物会与内置食物库**合并**，在手动录入的自动补全中**优先显示**。

#### 7.3 数据管理（备份与恢复）
- **导出所有数据（JSON）**：一键备份所有饮食、训练、体重、聊天记录至 `Downloads/增重助手备份_日期.json`。
- **导入数据（JSON）**：从备份文件恢复数据（清空现有表后批量插入）。
- **清理旧照片**：一键删除30天前的饮食缩略图，释放存储空间（数据库文字记录永久保留）。

#### 7.4 通知与个性化
- **"暴躁教练"提醒开关**：启用/关闭每日提醒。
- **提醒时间设置**：默认21:00，用户可自定义。
- **"暴躁语录"管理**：支持查看、添加、删除自定义的激励/嘲讽文案（App内随机抽取一条发送通知）。

---

## 4. 非功能需求

- **隐私与离线优先**：除AI识别和对话调用API外，所有数据均在本地，不上传任何第三方服务器。历史数据查看、手动录入、训练记录等完全离线可用。发送给AI的上下文中不包含任何个人身份信息，仅含食物名、训练动作名、重量、次数等脱敏数据。
- **性能**：冷启动时间 < 2秒，列表滑动无卡顿。所有本地计算（统计、路由）不得阻塞主线程，需使用协程。
- **网络**：AI模块需联网，网络异常时应有明确提示，不会崩溃。
- **成本控制**：图片压缩减小流量；对话Agent采用"按需查询"，Token消耗为全量投喂的1/10；统计值由SQL本地计算，不消耗API Token。
- **通知与集成**：
  - 支持利用**小米HyperOS焦点通知**能力（通过高优先级通知）模拟"灵动岛"展示训练倒计时。
  - 支持**Android桌面小组件**（使用Jetpack Glance，与Compose语法同源），展示当日热量进度与"暴躁教练"简讯，定时刷新（每30分钟或保存记录时主动更新）。

---

## 5. 技术选型

| 模块 | 技术方案 |
| :--- | :--- |
| 开发语言 | Kotlin |
| UI框架 | **Jetpack Compose + Material 3** |
| 导航 | Navigation Compose（单Activity架构，底部导航栏 + 各Tab内独立NavHost） |
| 状态管理 | ViewModel + StateFlow + collectAsState() |
| 本地数据库 | Room (SQLite) |
| 配置存储 | DataStore (Preferences) |
| 网络请求 | Retrofit + OkHttp |
| 图片压缩 | Bitmap.compress + 自定义长边缩放 |
| 图表 | **Vico**（Compose原生图表库，支持折线图/柱状图/主题定制） |
| AI API | 视觉模型：GLM-4V-Flash；文本模型：GLM-4-Flash / DeepSeek-V3（用户可配置） |
| 聊天界面 | **LazyColumn** + 自定义 Composable 消息气泡 |
| 后台任务 | WorkManager（定时清理照片、定时触发通知） |
| 桌面小组件 | Jetpack Glance（与 Compose 语法同源，学习成本为零） |
| 倒计时 | CountDownTimer + 系统通知（配合HyperIsland-ToolKit） |
| JSON解析 | Gson |
| 图片加载 | Coil（Compose原生图片加载库） |

---

## 6. API 规范与 Prompt 设计

### 6.1 食物识别
- **请求**：POST 到智谱 Chat Completion 接口（视觉模型）。
- **Prompt**：
  ```
  识别图中食物。估算每种食物的重量(g)和热量(kcal)。只返回纯JSON，不要任何解释。
  格式：{"foods":[{"name":"食物名","weight_g":数值,"calories_kcal":数值}],"total_calories":数值}
  ```
- **容错**：客户端提取第一个 `{` 到最后一个 `}` 之间的内容解析JSON。

### 6.2 每日综合评估
- **System Prompt**：专业增肌教练（CSCS）。
- **User Prompt**：拼接今日饮食/训练/体重数据（近期详细 + 远期统计值）+ "请用200字以内给出今日评估与建议"。

### 6.3 对话Agent（按需注入）
- **System Prompt**：专业私人健康与体能管家，可根据提供的用户近期数据给出个性化建议。
- **本地路由逻辑**：用户消息到达后，App本地通过关键词匹配决定查哪些数据。
- **上下文注入**：仅将本地查到的**精准数据**以"用户近期数据：..."形式拼入System Prompt之后，不注入全量历史。
- **用户消息**：直接发送用户原始问题。
- **滑动窗口**：每次请求携带最近10条聊天记录。

---

## 7. 数据模型设计（核心表）

| 表名 | 关键字段 |
| :--- | :--- |
| **DietRecord** | id, foodName, weightG, caloriesKcal, mealType, timestamp, imagePath |
| **TrainingRecord** | id, date, bodyParts, exerciseName, sets, reps, weightKg, notes |
| **BodyWeight** | id, date, weightKg |
| **ChatMessage** | id, role, content, timestamp |
| **AdviceLog** | id, date, requestSnapshot, aiResponse |
| **FoodLibrary**（新增） | id, name, caloriesPer100g, isCustom (Boolean) |
| **MealTemplate**（新增） | id, templateName, itemsJson (存食物列表) |

---

## 8. 项目包结构

```
com.example.health/
├── MainActivity.kt                 // 单Activity入口，NavHost根
├── HealthApp.kt                    // Application类
├── data/
│   ├── local/
│   │   ├── AppDatabase.kt          // Room数据库
│   │   ├── dao/                    // DAO接口
│   │   ├── entity/                 // 实体类
│   │   └── converter/              // 类型转换器
│   ├── repository/                 // 仓库层（统一数据访问）
│   ├── remote/
│   │   ├── api/                    // Retrofit API接口
│   │   └── dto/                    // 网络数据传输对象
│   └── preference/                 // DataStore配置
├── domain/
│   ├── model/                      // 领域模型
│   └── router/                     // AI对话意图路由引擎
├── ui/
│   ├── navigation/                 // 底部导航 + 路由定义
│   ├── diet/                       // 饮食记录Tab
│   │   ├── DietScreen.kt
│   │   ├── DietViewModel.kt
│   │   ├── FoodConfirmScreen.kt   // AI识别确认页
│   │   └── ManualInputScreen.kt   // 手动录入页
│   ├── training/                   // 训练记录Tab
│   │   ├── TrainingScreen.kt
│   │   ├── TrainingViewModel.kt
│   │   └── RestTimer.kt           // 组间休息倒计时组件
│   ├── chat/                       // AI对话Tab
│   │   ├── ChatScreen.kt
│   │   ├── ChatViewModel.kt
│   │   └── MessageBubble.kt       // 消息气泡组件
│   ├── dashboard/                  // 数据看板Tab
│   │   ├── DashboardScreen.kt
│   │   ├── DashboardViewModel.kt
│   │   └── charts/                 // Vico图表封装
│   ├── settings/                   // 设置页
│   │   ├── SettingsScreen.kt
│   │   ├── SettingsViewModel.kt
│   │   ├── FoodLibraryScreen.kt   // 自定义食物管理
│   │   └── ApiConfigScreen.kt     // API配置
│   ├── components/                 // 通用Composable组件
│   │   ├── ProgressBar.kt
│   │   ├── FoodItemCard.kt
│   │   └── ...
│   └── theme/                      // Material 3主题
├── widget/                         // Glance桌面小组件
│   └── CalorieWidget.kt
├── worker/                         // WorkManager后台任务
│   ├── PhotoCleanupWorker.kt
│   └── NotificationWorker.kt
└── util/                           // 工具类
    ├── ImageCompressor.kt
    ├── JsonParser.kt
    └── DateUtils.kt
```

---

## 9. 开发阶段规划（共7个阶段）

| 阶段 | 内容 | 预计耗时 |
| :--- | :--- | :--- |
| **Phase 0** | 引入全部依赖（Room/Retrofit/Vico/Coil/Glance/Navigation/DataStore/Gson）、建包结构、创建 Application 类 | 0.5天 |
| **Phase 1** | 底部导航4Tab（饮食/训练/对话/看板）+ Room全部建表（实体+DAO+Database）+ 设置页框架（DataStore） | 2天 |
| **Phase 2** | 饮食闭环：拍照→压缩→调API→确认修改→保存 + 内置食物库JSON解析 + 手动录入自动补全 + 模板CRUD | 3.5天 |
| **Phase 3** | 训练记录 + 历史动作推荐 + 组间休息倒计时（含震动/通知/上岛适配） + 每日综合评估 | 2.5天 |
| **Phase 4** | AI对话Agent：LazyColumn聊天界面 + 本地关键词路由 + 精准数据注入 + 滑动窗口（最近10条） | 2天 |
| **Phase 5** | 体重录入、目标设置、Vico图表看板、数据备份/恢复（JSON导入导出）、照片自动清理（WorkManager） | 2.5天 |
| **Phase 6** | 桌面小组件（Glance）、暴躁教练通知（WorkManager + 随机语录）、UI美化、异常处理完善 | 1.5天 |
| **Phase 7** | 全面测试（真机/模拟器）、性能优化、Log清理、发布准备 | 2天 |

**总预估工期：约 16.5 天（全职开发）**

---

## 10. 验收标准（MVP交付）

1. **饮食识别**：拍一碗米饭，AI识别后弹出确认界面，可手动调整重量和热量，最终保存热量在200-250kcal范围。
2. **自定义食物库**：在设置页添加"妈妈牌红烧肉 250kcal/100g"，手动录入时输入"红烧"自动补全并填入热量。
3. **训练记录**：记录"深蹲 50kg 4x10"，主页显示今日训练部位"腿"；下次选择腿部位时，"深蹲"出现在推荐历史动作中。
4. **休息倒计时**：点击"开始休息"后，圆环倒计时从60秒开始递减（Canvas绘制），结束时手机震动。
5. **每日评估**：设置目标2800kcal，当日摄入2100kcal时，评估能指出热量缺口并给出加餐建议。
6. **对话Agent**：
   - 问"我最近三天吃了多少热量？" → 能基于实际记录回答精确数值（本地路由命中"热量"，查DietRecord）。
   - 问"我深蹲重量进步了吗？" → 能提取近期深蹲数据分析趋势（本地路由命中"深蹲"，查TrainingRecord）。
   - 对话历史UI显示全部记录，但API调用仅发送最近10条。
7. **图表**：连续记录3天体重和饮食，Vico折线图正确展示趋势，柱状图与记录一致。
8. **备份恢复**：导出JSON文件后，清空数据再导入，所有记录完整恢复。
9. **暴躁教练通知**：当天摄入<目标80%时，在设定时间收到本地通知，文案从语录库随机抽取。

---

## 11. 附录：扩展可能性（非MVP需求）

- 蛋白质/碳水/脂肪宏量营养素估算与统计。
- 训练模板一键加载完整训练计划（如"五分化训练"）。
- 云端备份与多设备同步（需自行搭建存储方案）。
- 语音输入饮食记录。
- Wear OS手表端快速记录体重与训练。

---

## 附录A：V4.0 → V4.1 变更清单

| 变更项 | V4.0 (旧) | V4.1 (新) | 理由 |
| :--- | :--- | :--- | :--- |
| UI框架 | XML (Android Views) + Fragment | Jetpack Compose + Material 3 | 代码量减半，聊天/表单/动态列表实现效率大幅提升 |
| 图表库 | MPAndroidChart | Vico | Compose原生图表库，无需AndroidView包装 |
| 聊天列表 | RecyclerView + Adapter | LazyColumn | Compose原生列表组件，无需Adapter样板 |
| 导航方式 | FragmentManager + NavHost | Navigation Compose | 类型安全的声明式导航 |
| 图片加载 | 未指定 | Coil | Compose原生图片加载，支持占位图/缓存/裁剪 |
| 导航架构 | 多Activity | 单Activity | Compose最佳实践，导航更流畅 |
| 新增 | — | 完整包结构定义（第8节） | 指导Phase 0项目搭建 |
